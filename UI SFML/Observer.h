#ifndef OBSERVER
#define OBSERVER

#include <iostream>
#include <vector>
#include "FTP.h"
/*
class Observer
{
public:
    FTP* model;
    int denom;
public:
    Observer(FTP* mod, int div) {
        model = mod;
        denom = div;
        // 4. Наблюдатели регистрируются у субъекта
        model->attach(this);
    }
    virtual void update() = 0;
protected:
    FTP* getSubject() {
        return model;
    }
    int getDivisor() {
        return denom;
    }
};
*/
#endif
